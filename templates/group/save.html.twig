{% extends 'base.html.twig' %}

{% block body %}

    <div class="container h-full max-w-lg mx-auto flex flex-col gap-5">
        <h1 class="py-2 text-3xl text-center font-extrabold md:text-5xl lg:text-6x text-transparent bg-clip-text bg-gradient-to-r to-emerald-600 from-sky-400">
            {{ group.id ? 'Modifier le groupe ' ~ group.name : "Créer un groupe" }}        </h1>
        <div class="overflow-y-auto p-8">
            {{ form_start(form) }}

            {{ form_row(form.name) }}
            {{ form_row(form.Users) }}
        </div>

        <div class="flex justify-center items-center">
            <button type="submit"
                    class="text-white bg-gradient-to-br from-purple-600 to-blue-500 hover:bg-gradient-to-bl focus:ring-4 focus:outline-none focus:ring-blue-300 dark:focus:ring-blue-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center me-2 mb-2">
                {{ group.id ? "Modifer" : "Créer" }}
            </button>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}



{% block title %}

{% endblock %}

